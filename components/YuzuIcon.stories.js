import React from "react";

import YuzuIcon from "./YuzuIcon";

export default {
  title: "YuzuIcon",
  component: YuzuIcon,
};

const Template = (args) => <YuzuIcon {...args} />;

export const Default = Template.bind({});
